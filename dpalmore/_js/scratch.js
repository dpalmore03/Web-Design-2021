// this is scratch paper for programmers

let cpu= ["rock", "paper", "scissors"];

function randchice(x){
    return Math.floor(Math.random()*x);
}

letcpuChoice = randChice(cpu.length);
let playerChoice = randChoice(cpu.length);

console.log(cpu)


